@inject IViewInventoriesByNameUseCase ViewInventoriesByNameUseCase

<div class="input-group">
	<input class="form-control"
	placeholder="Type something here to search"
	@bind-value="this.searchFilter">
	</input>
	<button type="submit" class="btn-success input-group-text">Search</button>
</div>

@code {
	private string searchFilter = string.Empty;
	private List<Inventory> inventories = new List<Inventory>();

	private async Task HandleSearch()
	{
		var ivs = (await ViewInventoriesByNameUseCase.ExecuteAsync(this.searchFilter)).ToList();
	}

}
